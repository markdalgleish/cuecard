var cuecard = require('cuecard'),
	path = require('path');

cuecard.create({
	adapter: '<%=adapter%>',

	port: <%=port%>,
	<% if (hasRemote) { %>remoteUrl: '<%=remoteUrl%>',<% } %>

	title: '<%=title%>',

	slides: path.join(__dirname, 'slides.html'),
	styles: path.join(__dirname, 'slides.css')
});